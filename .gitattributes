# Generated by pytest-textual-snapshot / Rich's SVG export. The template
# itself emits trailing whitespace on some lines; hand-editing it to satisfy
# `git diff --check` would make the committed fixture differ from what every
# future render of the same app produces, breaking the snapshot comparison
# it exists to make possible. Excluded from whitespace checks rather than
# "fixed".
tests/tui/__snapshots__/**/*.svg -whitespace
